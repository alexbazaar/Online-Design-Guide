import React from "react";
import FieldSet from "./index";

module.exports = (
  <FieldSet
    name="options"
    showLegend={false}
    legend="legend"
  >
    <legend
      data-component="legend"
      className="legend">
      Which option do you prefer?
    </legend>
  </FieldSet>

);
