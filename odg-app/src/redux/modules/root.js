import { combineReducers } from 'redux';

// eslint-disable-next-line require-jsdoc, no-unused-vars
function tmpReducer(state = {}, action) { return state; }

const rootReducer = combineReducers({
  tmpReducer
});

export default rootReducer;
